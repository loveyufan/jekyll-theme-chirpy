{
  "build": {
    "env": {
      "LC_ALL": "C.UTF-8",
      "LANG": "C.UTF-8",
      "JEKYLL_ENV": "production"
    }
  },
  "installCommand": "bundle config set --local path vendor/bundle && bundle install",
  "buildCommand": "bundle exec jekyll build",
  "outputDirectory": "_site"
}
